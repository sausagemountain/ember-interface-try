<ul class="tree-view">
  {{#each @tree as |item|}}
    <TreeviewInternal @node={{item}} @items={{item.items}} @parent={{this}} @nonDraggable={{item.items}}>
      {{#if item.node}}
        {{item.node}}
      {{else}}
        {{item}}
      {{/if}}
    </TreeviewInternal>
  {{/each}}
</ul>